# Dora Matcha Bar

Simple static site for Dora Matcha Bar. Contains `index.html` and an `images/` folder.

To push this project to GitHub:

1. Create a new repository on GitHub (web UI or `gh repo create`).
2. Add the remote and push:

```bash
git remote add origin git@github.com:<your-username>/<repo>.git
git branch -M main
git push -u origin main
```

If you want me to create the GitHub repo for you, tell me the repository name and whether it should be public or private.
